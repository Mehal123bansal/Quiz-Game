# React Quiz Game

A simple and interactive quiz game built with React. Test your knowledge with multiple-choice questions and enjoy a clean, responsive user interface.

## Features

- Multiple-choice questions
- Score tracking
- Interactive and responsive design
- Built with React and modern JavaScript

## Installation

1. Clone the repository:
   ```bash
   git clone https://github.com/Mehal123bansal/quiz-game.git
Navigate to the project folder:

bash
Copy code
cd react-game
Install dependencies:

bash
Copy code
npm install
Start the development server:

bash
Copy code
npm start
The app will run on http://localhost:3000 by default.

Usage
Open the app in your browser.

Select an answer for each question.

Your score will be displayed at the end of the quiz.

Technologies Used
React

JavaScript (ES6+)

HTML5 & CSS3
